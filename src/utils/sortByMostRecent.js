import listenHistory from "../data/listen_history.json" assert { type: "json" };

function sortByMostRecent(history) {
    console.log("hi");
}

sortByMostRecent();

// export default sortByMostRecent;